class UserController {
  userList = (req, res, next) => {};
  registerUser = (req, res, next) => {
    //
    //form d
    let data = {};
    //name, email, password, role[admin, seller, customer], image
    //validation
    try {
    } catch (error) {
      //
    }
  };
}

module.exports = UserController;
